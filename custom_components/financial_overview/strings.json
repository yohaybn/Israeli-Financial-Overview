{
  "config": {
    "step": {
      "user": {
        "title": "Financial Overview",
        "description": "Connect to Financial Overview via MQTT (same settings as Configuration → MQTT in the app).",
        "data": {
          "command_secret": "Command secret",
          "device_id": "Device ID",
          "command_topic": "Command topic",
          "state_prefix": "State topic prefix"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to MQTT broker"
    },
    "abort": {
      "already_configured": "Already configured"
    }
  }
}
