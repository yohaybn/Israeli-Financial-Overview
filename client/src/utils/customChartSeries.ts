/** Re-export shared implementation (dashboard + PDF use the same aggregation rules). */
export {
    buildCustomChartSeries,
    getTransactionsForUserChart,
    passesUserChartFilters,
} from '@app/shared';
